using System.Collections;
using UnityEngine;

public class SoilWallDestroyEffect : MonoBehaviour
{

    // Update is called once per frame
    void Update()
    {
        Destroy(gameObject,3F);
    }
}
